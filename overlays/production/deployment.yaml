apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    environment: production
  name: acme-node
spec:
  selector:
    matchLabels:
      environment: production
  template:
    metadata:
      labels:
        environment: production
